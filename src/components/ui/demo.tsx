import HeroSection from '@/components/ui/glassmorphism-trust-hero';

export default function HeroDemo() {
  return (
    <div className="h-screen w-full overflow-y-auto bg-zinc-950">
      <HeroSection />
    </div>
  );
}
